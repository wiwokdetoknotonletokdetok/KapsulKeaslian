name: Deploy

on:
  workflow_run:
    workflows: ["Unit Tests"]
    types:
      - completed
    branches:
      - main

jobs:
  deploy:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build Docker image
        run: docker build -t kapsulkeaslian:latest .

      - name: Save Docker image to file
        run: docker save kapsulkeaslian:latest -o kapsulkeaslian.tar

      - name: Create .env file from secrets
        run: |
          cat <<EOF > .env
          DB_NAME=${{ secrets.DB_NAME }}
          DB_USERNAME=${{ secrets.DB_USERNAME }}
          DB_PASSWORD=${{ secrets.DB_PASSWORD }}
          DB_HOST=${{ secrets.DB_HOST }}
          DB_PORT=${{ secrets.DB_PORT }}
          JWT_SECRET=${{ secrets.JWT_SECRET }}
          EOF

      - name: Prepare deploy files
        run: |
          mkdir kapsulkeaslian
          mv kapsulkeaslian.tar .env kapsulkeaslian/

      - name: Copy Docker image and .env to server
        uses: appleboy/scp-action@v0.1.4
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          key: ${{ secrets.SSH_KEY }}
          source: "kapsulkeaslian/*"
          target: "~/"

      - name: Deploy on remote server
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          key: ${{ secrets.SSH_KEY }}
          script: |
            cd ~/kapsulkeaslian
            
            docker stop kapsulkeaslian || true
            docker rm kapsulkeaslian || true
            
            docker load -i kapsulkeaslian.tar
            
            docker run -d \
              --name kapsulkeaslian \
              --env-file ~/kapsulkeaslian/.env \
              -p 8080:8080 \
              kapsulkeaslian:latest
